name: 'Automate deploy on Netlify'

on:
  push:
    branches:
      - main

jobs:
  first-job:
    name: 'First'

    runs-on: 'ubuntu-latest'

    steps:
      - name: 'Say Hello wolrd'
        shell: bash
        run: |
          echo "hello world"
